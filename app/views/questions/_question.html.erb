<%= question.body %>
  <%= link_to 'Show', question_path(question.id), :method => :get %>
  <%= link_to 'Edit', edit_question_path(question.id), :method => :get %>
  <%= link_to 'Delete', question_path(question.id), :method => :delete, data: { confirm: 'Are you sure?' } %><br>
<%= render question.answers %>
