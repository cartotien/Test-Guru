<table class="table table-striped table-hover table-responsive table-bordered">
  <thead class="thead-dark">
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.questions') %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </tr>
 </thead>

  <%= render @tests %>

  <%= link_to t('.new_test'), new_admin_test_path, :method => :get, class: "btn btn-primary btn-sm" %>

</table>
