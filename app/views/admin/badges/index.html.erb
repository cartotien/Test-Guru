<%= render 'shared/error_handle', resource: @test %>

<table class="table table-bordered">
  <thead>
    <tr>
      <th><%= t('.image') %></th>
      <th><%= t('.name') %></th>
      <th colspan="2"><%= t('.actions') %></th>
    </tr>
 </thead>

  <%= render @badges %>

  <%= link_to t('.new_badge'), new_admin_badge_path, class: "btn btn-primary btn-sm" %>

</table>
