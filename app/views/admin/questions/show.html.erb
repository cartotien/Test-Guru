<p><%= @question.body %></p>

<%= link_to t('back'), admin_test_path(@question.test.id), :method => :get, class: "btn btn-secondary btn-sm"  %>
<%= link_to t('edit'), edit_admin_question_path, :method => :get, class: "btn btn-secondary btn-sm"  %>
<%= link_to t('delete'), admin_question_path, :method => :delete, class: "btn btn-secondary btn-sm"  %>
<%= link_to t('.create_answer'), new_admin_question_answer_path(@question), class: "btn btn-secondary btn-sm"  %>
<div class='container mt-2'>
<%= render @question.answers %>
</div>
