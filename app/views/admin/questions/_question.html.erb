<%= question.body %>
  <%= link_to 'Show', admin_question_path(question.id), :method => :get %>
  <%= link_to 'Edit', edit_admin_question_path(question.id), :method => :get %>
  <%= link_to 'Delete', admin_question_path(question.id), :method => :delete, data: { confirm: 'Are you sure?' } %><br>
<%= render question.answers %>
